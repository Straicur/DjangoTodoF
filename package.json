{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.1",
    "@testing-library/react": "^12.1.2",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^0.21.1",
    "bootstrap": "^4.6.0",
    "cross-env": "^7.0.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "5.0.0",
    "reactstrap": "^8.9.0",
    "web-vitals": "^2.1.3"
  },
  "scripts": {
    "start": "cross-env REACT_APP_API_ADDRESS=${API_ADDRESS} REACT_APP_PORT=${PORT} react-scripts start",
    "build": "cross-env REACT_APP_API_ADDRESS=${API_ADDRESS} REACT_APP_PORT=${PORT} react-scripts build",
    "eject": "cross-env REACT_APP_API_ADDRESS=${API_ADDRESS} REACT_APP_PORT=${PORT} react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
